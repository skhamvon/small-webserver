# Remote **abtest-solution** (AbTestSlot réel, modale simulation). Nécessite l’API + ce remote (ex. `npm run dev` à la racine du repo parent, ou `dev:api` + `dev:remote` dans abtest-solution).
VITE_ABTEST_REMOTE_URL=http://localhost:5001/remoteEntry.js
#
# Remote stub **uniquement small-webserver** (sans abtest-solution) :
# VITE_ABTEST_REMOTE_URL=http://localhost:5101/remote/remoteEntry.js
