# Dependencies
node_modules/

# Build outputs (Vite, tsc serveur, etc.)
dist/

# Environment
.env
.env.*.local
*.local
# Overrides locaux par app (ex. apps/remote/.env) — les modèles restent en *.example
apps/**/.env
!apps/host/.env.development
!apps/host/.env.production

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript (build incrémental / composite)
*.tsbuildinfo

# Tests
coverage/

# Outils
.eslintcache
.turbo/

# Vite (cache projet si hors node_modules)
.vite/

# Module Federation — généré par le plugin Vite / DTS
.__mf__temp/
@mf-types/
apps/abtest-remote/.mf/
